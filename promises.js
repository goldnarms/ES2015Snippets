var promise = new Promise((resolve, reject)=>{
    var request = new XMLHttpRequest();
    var url = "data.json";
    request.open("GET", url);
    if(request.addEventListener("load", ()=>{
        
    }
});